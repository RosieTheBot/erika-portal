// STR Matchmaker sync stub for Vercel deployment

export const strSync = {
  async syncTopPicksToMatchmaker(properties: any[]) {
    console.log('STR sync stub - implement with real sync:', properties.length);
    return { synced: 0 };
  },
};
